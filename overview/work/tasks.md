# Tareas Ecosistémicas (`tasks.md`)

## Tarea Activa: [w5] Diseño y Creación de `multicore-agent-rules` (Monorepo & Multi-Stack Orchestrator)

- **Tipo**: Core Architecture / Orchestration
- **Prioridad**: Alta
- **Cores Afectados**: MultiCore / Web / Rust / Go / Python / Backend / Flutter / Transversal
- **Hipótesis / Objetivo**:
  1. Diseñar el estándar y arquitectura para `multicore-agent-rules` que orqueste múltiples `*-agent-rules` en proyectos políglotas (ej. Frontend Web/Svelte + Backend Rust).
  2. Definir el sistema de federación de `overview/` y `architecture/` para cruzar contratos de API, schemas, DTOs compartidos y flujos end-to-end.
  3. Establecer ubicación canónica en el ecosistema (`MultiCore/multicore-agent-rules`).
  4. Sincronizar estándares (`ESTRUCTURA_CLONACION.md`, `tracker.md`, `RULES_STANDARD.md`).


