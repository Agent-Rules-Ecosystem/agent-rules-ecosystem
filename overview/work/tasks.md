# Tareas Ecosistémicas (`tasks.md`)

## Tarea Activa: [w2] Estandarización de READMEs en el Ecosistema

- **Tipo**: Refactor / Gobernanza
- **Prioridad**: Alta
- **Hipótesis / Objetivo**:
  1. Eliminar `mkdir -p` de los bloques de código de instalación para permitir copia limpia del comando en 1 solo click.
  2. Forzar URLs SSH (`git@github.com:xolotl-hub/...`) en lugar de HTTPS en todos los README.
  3. Renombrar sección a **Quick Start** con 3 bloques de código independientes:
     - Bloque 1: Comando Git (`git submodule add git@github.com:...`)
     - Bloque 2: Trigger Agente (`$boot`)
     - Bloque 3: Ejemplo de comando (`$work crear componente de botón reutilizable`)
  4. Homogeneizar el orden de secciones en todos los README de Cores y Skills.
